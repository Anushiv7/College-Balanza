# Postgres connection string (required for persisted comparison history)
DATABASE_URL=postgres://user:pass@host:5432/college_balanza

# OpenAI-compatible LLM gateway. Defaults shown.
OPENAI_API_KEY=sk-replace-me
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Optional: override the anonymous-id cookie name
# ANON_COOKIE_NAME=cb_anon_id
